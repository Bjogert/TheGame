# Copy this file to `secrets.env` and fill in your credentials before running the game.
# The dialogue plugin reads these environment variables on startup. Leaving the
# API key blank keeps the deterministic local fallback active.

# Required for live OpenAI calls
OPENAI_API_KEY=

# Optional overrides (uncomment and adjust as needed)
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com
# OPENAI_TEMPERATURE=0.7
# OPENAI_MAX_OUTPUT_TOKENS=220
# OPENAI_TIMEOUT_SECS=15
